<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="item"
            type="pro.haichuang.learn.home.ui.activity.find.itemmodel.CommentModel" />
    </data>

    <pro.haichuang.learn.home.ui.weight.CommentTextView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/color66"
        android:textSize="15sp"
        app:content="@{item.text}"
        app:name="@{item.commenter.realname}"
        app:repeatName="@{item.parentName}" />
</layout>