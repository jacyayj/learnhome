<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="item"
            type="pro.haichuang.learn.home.ui.activity.index.itemmodel.ItemPingGuModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@drawable/divider_horizontal_e0"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:padding="10dp"
        android:showDividers="middle">

        <TextView
            style="@style/text_15_00"
            android:text="@{item.name}"
            android:paddingBottom="15dp"
            android:textColor="@color/color00A6FF" />

        <pro.haichuang.learn.home.ui.weight.AutoGridView
            android:id="@+id/grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:verticalSpacing="10dp"
            android:numColumns="2" />
    </LinearLayout>
</layout>